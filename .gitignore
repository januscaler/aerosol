# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies (root app + any subfolder)
node_modules/

# Frontend build (root Vite → Tauri `frontendDist`)
dist
dist-ssr

# Rust / Cargo
target/
**/target/

# Python
.venv

# Env / secrets (keep examples)
.env
.env.*
!.env.example
*.local

# --- Website (VitePress): build output, dependency pre-bundles, local env ---
website/node_modules/
website/.vitepress/dist/
website/.vitepress/cache/
website/.env
website/.env.*
!website/.env.example

# Generated in CI from GitHub API; copied into the static site for download links.
website/public/latest-release.json

# VitePress under other paths in the future
**/.vitepress/dist/
**/.vitepress/cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
